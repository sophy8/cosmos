import { AvailablePlanetToPipe } from './available-planet-to.pipe';

describe('AvailablePlanetToPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailablePlanetToPipe();
    expect(pipe).toBeTruthy();
  });
});
