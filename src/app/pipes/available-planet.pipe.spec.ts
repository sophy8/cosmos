import { AvailablePlanetPipe } from './available-planet.pipe';

describe('AvailablePlanetPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailablePlanetPipe();
    expect(pipe).toBeTruthy();
  });
});
